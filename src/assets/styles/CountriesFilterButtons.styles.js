import {StyleSheet} from 'react-native';

const CountriesFilterButtonsStyles = StyleSheet.create({
  button: {
    color: '#DBDBDB',
    fontWeight: '700',
    marginRight: 32,
  },
});

export default CountriesFilterButtonsStyles;
